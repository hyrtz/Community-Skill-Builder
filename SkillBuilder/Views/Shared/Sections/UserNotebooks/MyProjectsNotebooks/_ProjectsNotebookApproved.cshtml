@model List<SkillBuilder.Models.CourseProjectSubmission>

<style>
    /* === Accepted Projects Section === */
    .user-accepted-projects-section {
        display: flex;
        flex-direction: column;
        gap: 1.5rem;
    }

    .user-accepted-projects-grid {
        display: flex;
        flex-wrap: wrap;
        gap: 1.5rem;
    }

    .user-accepted-projects-card {
        width: 200px;
        text-align: center;
        cursor: pointer;
    }

        .user-accepted-projects-card img {
            width: 100%;
            height: 160px;
            object-fit: cover;
            border-radius: 10px;
            box-shadow: 0 2px 6px rgba(0,0,0,0.15);
            transition: transform 0.2s ease;
            border: 3px solid #28A745; /* green border for accepted */
        }

            .user-accepted-projects-card img:hover {
                transform: scale(1.05);
            }

    .user-accepted-projects-course-name {
        margin-top: 0.5rem;
        font-weight: bold;
        color: #28A745;
        font-size: 0.95rem;
    }

    .user-accepted-projects-empty {
        padding: 2rem;
        text-align: center;
        color: #777;
        font-size: 1rem;
        line-height: 1.6;
        border: 1px dashed #ccc;
        border-radius: 10px;
        background-color: #fafafa;
    }

    /* Modal Styles */
    .accepted-projects-modal {
        display: none;
        position: fixed;
        z-index: 9999;
        left: 0;
        top: 0;
        width: 100%;
        height: 100%;
        background-color: rgba(0,0,0,0.7);
        justify-content: center;
        align-items: center;
    }

    .accepted-projects-modal-content {
        background: #fff;
        border-radius: 10px;
        max-width: 700px;
        width: 90%;
        padding: 1.5rem;
        position: relative;
        animation: fadeIn 0.3s ease-in-out;
    }

    .accepted-projects-close {
        position: absolute;
        top: 10px;
        right: 15px;
        font-size: 1.5rem;
        font-weight: bold;
        color: #333;
        cursor: pointer;
    }

    .accepted-projects-modal-body {
        text-align: center;
    }

    .accepted-projects-image {
        max-width: 100%;
        max-height: 400px;
        margin-bottom: 1rem;
        border-radius: 10px;
    }

    .accepted-projects-details {
        text-align: left;
    }

        .accepted-projects-details h2 {
            font-size: 1.5rem;
            font-weight: bold;
            margin-bottom: 0.5rem;
            color: #28A745;
        }

        .accepted-projects-details p {
            margin: 0.2rem 0;
            font-size: 1rem;
            color: #444;
        }
</style>

<section class="user-accepted-projects-section">

    @if (Model != null && Model.Any())
    {
        <div class="user-accepted-projects-grid">
            @foreach (var project in Model)
            {
                <div class="user-accepted-projects-card"
                     data-image="@project.MediaUrl"
                     data-title="@project.Course?.Title"
                     data-caption="@project.Description">
                    <img src="@project.MediaUrl" alt="Accepted project from @project.Course?.Title" />
                    <div class="user-accepted-projects-course-name">@project.Course?.Title</div>
                </div>
            }
        </div>
    }
    else
    {
        <div class="user-accepted-projects-empty">
            You have no approved projects yet.<br />
            Once your submission is approved, it will show up here.
        </div>
    }
</section>

<!-- Modal -->
<div id="acceptedProjectsModal" class="accepted-projects-modal">
    <div class="accepted-projects-modal-content">
        <span class="accepted-projects-close">&times;</span>
        <div class="accepted-projects-modal-body">
            <img id="acceptedProjectsImage" class="accepted-projects-image" src="" alt="Project Image" />
            <div class="accepted-projects-details">
                <h2 id="acceptedProjectsTitle"></h2>
                <p id="acceptedProjectsCaption"></p>
            </div>
        </div>
    </div>
</div>

<script>
    document.addEventListener("DOMContentLoaded", () => {
        const modal = document.getElementById("acceptedProjectsModal");
        const modalImage = document.getElementById("acceptedProjectsImage");
        const modalTitle = document.getElementById("acceptedProjectsTitle");
        const modalCaption = document.getElementById("acceptedProjectsCaption");
        const closeBtn = document.querySelector(".accepted-projects-close");

        document.querySelectorAll(".user-accepted-projects-card").forEach(card => {
            card.addEventListener("click", () => {
                modalImage.src = card.dataset.image;
                modalTitle.textContent = card.dataset.title;
                modalCaption.textContent = card.dataset.caption;
                modal.style.display = "flex";
            });
        });

        closeBtn.addEventListener("click", () => modal.style.display = "none");
        modal.addEventListener("click", (e) => {
            if (e.target === modal) modal.style.display = "none";
        });
    });
</script>