@model SkillBuilder.Models.Course

<style>
    .mentor-heading {
        font-size: 2rem;
        margin-bottom: 2rem;
        color: #222;
    }

    .mentor-profile {
        display: flex;
        align-items: center;
        gap: 2rem;
        margin-bottom: 2rem;
    }

    .mentor-avatar {
        width: 120px;
        height: 120px;
        object-fit: cover;
        border-radius: 50%;
        border: 4px solid #ccc;
    }

    .mentor-info {
        display: flex;
        flex-direction: column;
        justify-content: center;
    }

    .mentor-name {
        font-size: 1.5rem;
        font-weight: bold;
        color: #344AEA;
        text-decoration: none;
    }

        .mentor-name:hover {
            text-decoration: underline;
        }

    .mentor-sub {
        font-size: 1rem;
        color: #555;
    }

    .mentor-bio {
        font-size: 1rem;
        color: #333;
        line-height: 1.7;
    }
</style>

@{
    var artisan = Model.Artisan;
    var fullName = $"{artisan.FirstName} {artisan.LastName}";
}

<h1 class="mentor-heading">Get to know your mentor</h1>

@if (artisan != null)
{
    <div class="mentor-profile">
        <img src="/assets/Avatar/Sample10.svg" class="mentor-avatar" />
        <div class="mentor-info">
            <a href="/ArtisanViewAsMentor/@artisan.ArtisanId" class="mentor-name">@fullName</a>
            <p class="mentor-sub">@artisan.Profession from @artisan.Hometown</p>
        </div>
    </div>

    <p class="mentor-bio">
        @artisan.Introduction
    </p>
}
else
{
    <p>Mentor information not available.</p>
}