@model List<SkillBuilder.Models.CourseProjectSubmission>

<style>
    .artisan-accepted-review-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
        gap: 1.5rem;
    }

    .artisan-accepted-review-card {
        display: flex;
        flex-direction: column;
        background: #fff;
        border: 1px solid #ddd;
        border-radius: 12px;
        box-shadow: 0 2px 6px rgba(0,0,0,0.08);
        overflow: hidden;
        cursor: pointer;
        padding: 1rem;
        max-height: 310px;
        min-height: 310px;
        max-width: 270px;
        min-width: 270px;
        transition: transform 0.2s ease, box-shadow 0.2s ease;
    }

        .artisan-accepted-review-card:hover {
            transform: translateY(-4px);
        }

        .artisan-accepted-review-card img {
            width: 100%;
            height: 160px;
            object-fit: cover;
            border-radius: 8px;
            margin-bottom: 0.75rem;
        }

    .artisan-accepted-review-info {
        font-size: 0.95rem;
    }

    .artisan-accepted-review-course {
        font-weight: bold;
        color: #555;
        margin-bottom: 0.3rem;
        display: -webkit-box;
        -webkit-line-clamp: 1;
        -webkit-box-orient: vertical;
        overflow: hidden;
        text-overflow: ellipsis;
    }

    .artisan-accepted-review-desc {
        color: #555;
        font-size: 0.9rem;
        display: -webkit-box;
        -webkit-line-clamp: 2;
        -webkit-box-orient: vertical;
        overflow: hidden;
        text-overflow: ellipsis;
    }

    .artisan-accepted-review-status {
        background-color: green;
        border: solid green 1px;
        border-radius: 6px;
        color: white;
        font-weight: 600;
        padding: 0.5rem;
        text-align: center;
        width: 50%;
        margin-top: 1rem;
    }

    .artisan-accepted-review-empty {
        text-align: center;
        color: #777;
        font-style: italic;
        margin: 2rem 0;
    }
</style>

<div class="artisan-accepted-review-grid">

    @if (Model != null && Model.Count > 0)
    {
        @foreach (var submission in Model)
        {
            <div class="artisan-accepted-review-card"
                 data-id="@submission.Id"
                 data-image="@submission.MediaUrl"
                 data-title="@submission.Title"
                 data-caption="@submission.Description"
                 data-course="@submission.Course?.Title"
                 data-status="@submission.Status">

                <img src="@submission.MediaUrl" alt="Accepted project from @submission.Course?.Title" />

                <div class="artisan-accepted-review-info">
                    <div class="artisan-accepted-review-course">Course Title: @submission.Course?.Title</div>
                    <div class="artisan-accepted-review-desc"><span style="font-weight: 600;">Description: </span>@submission.Description</div>
                    <div class="artisan-accepted-review-status">@submission.Status</div>
                </div>
            </div>
        }
    }
</div>

@if (Model == null || Model.Count == 0)
{
    <div class="artisan-accepted-review-empty">
        No accepted projects yet.
    </div>
}